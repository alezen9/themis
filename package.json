{
  "name": "themis",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter frontend dev",
    "ndg:draft-to-nodes": "node scripts/ndg-draft-to-nodes.mjs",
    "ndg:nodes-to-draft": "node scripts/ndg-nodes-to-draft.mjs",
    "typecheck": "pnpm -r typecheck",
    "validate": "pnpm -r --if-present validate",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@tanstack/eslint-plugin-router": "^1.114",
    "eslint": "^9",
    "eslint-config-prettier": "^10",
    "eslint-plugin-react": "^7",
    "eslint-plugin-react-hooks": "^5",
    "eslint-plugin-react-refresh": "^0.4",
    "prettier": "^3",
    "typescript": "^5.7",
    "typescript-eslint": "^8"
  }
}
